export enum AppMode {
    TEST = "test",
    LIVE = "live",
}
export enum WebhookStatus {
    PENDING = "pending",
    SUCCESSFUL = "successful",
    FAILED = "failed",
}
export enum WebhookType {
    INTERNAL = "internal",
    EXTERNAL = "external"
}